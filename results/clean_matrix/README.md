# Clean seed-42 records

This directory contains compact JSON records from the clean one-step matrix.
The training configuration is 50 epochs maximum with patience 10, target-
disjoint validation, train-only min--max normalization, and observed history
as the primary evaluation protocol.  Model checkpoints are included only for
the clean V0.1 runs; full historical checkpoints and logs remain local-only.

